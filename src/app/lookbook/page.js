import LookbookClient from './LookbookClient';

export const metadata = {
    title: 'Ewa Lookbook | Royal Haven',
    description: 'Explore the definitive hair, makeup, gele, and fashion lookbook of Ewa Artistry.',
};

export default function LookbookPage() {
    return (
        <main>
            <LookbookClient />
        </main>
    );
}
